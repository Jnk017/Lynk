import React from 'react';
import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function DiscoveryFeedScreen() {
  return (
    <PlaceholderScreen
      title="Discovery Feed"
      description="The video discovery feed is not part of the Sprint 1 stabilization scope, but this route now resolves safely."
      todo="Implement feed ranking, media playback, and moderation gates."
    />
  );
}
