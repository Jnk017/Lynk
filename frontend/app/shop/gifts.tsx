import React from 'react';
import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function GiftStoreScreen() {
  return (
    <PlaceholderScreen
      title="Gift Store"
      description="The gift catalog endpoint exists. The purchase and send flow is staged behind this safe placeholder."
      todo="Fetch the gift catalog, add purchase confirmation, and send receipts."
    />
  );
}
