import React from 'react';
import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Account and privacy settings will be added here without blocking core alpha navigation."
      todo="Add notification, privacy, account, and logout-all controls."
    />
  );
}
