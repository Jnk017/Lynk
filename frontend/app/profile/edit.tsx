import React from 'react';
import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function ProfileEditScreen() {
  return (
    <PlaceholderScreen
      title="Edit Profile"
      description="Profile editing is available in the backend and will be wired to media, prompts, and preferences in a follow-up sprint."
      todo="Connect this screen to /users/me and profile media endpoints."
    />
  );
}
