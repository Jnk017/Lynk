import React from 'react';
import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function StakingScreen() {
  return (
    <PlaceholderScreen
      title="Anti-Ghosting Staking"
      description="Anti-ghosting staking is available as a backend workflow and is intentionally staged before mobile rollout."
      todo="Add stake creation, attendance confirmation, and settlement status."
    />
  );
}
